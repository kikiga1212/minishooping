### 📘 회원 테이블 (MemberEntity)
| 필드명         | 타입               | 설명                                                     |
| ----------- | ---------------- | ------------------------------------------------------ |
| `id`        | Long             | 회원 고유 ID (PK, 자동 생성)                                   |
| `username`  | String           | 사용자명 (로그인 ID, 최대 50자, 유일)                              |
| `password`  | String           | 비밀번호 (암호화 저장, 최대 100자)                                 |
| `name`      | String           | 회원 이름 (최대 50자)                                         |
| `email`     | String           | 이메일 주소 (최대 100자, 유일)                                   |
| `phone`     | String           | 전화번호 (최대 20자)                                          |
| `address`   | String           | 주소 (최대 200자, 선택 입력 가능)                                 |
| `createdAt` | LocalDateTime    | 가입일시 (자동 생성, 수정 불가)                                    |
| `updatedAt` | LocalDateTime    | 수정일시 (자동 갱신)                                           |
| `status`    | String           | 회원 상태 (ACTIVE, INACTIVE, DELETED, 기본값: ACTIVE, 최대 20자) |
| `carts`     | List<CartEntity> | 회원이 가진 장바구니 목록 (1:N 관계, Cascade.ALL, orphanRemoval 적용) |

### 📘 상품 테이블 (ProductEntity)
| 필드명             | 타입               | 설명                                                                       |
| --------------- | ---------------- | ------------------------------------------------------------------------ |
| `id`            | Long             | 상품 고유 ID (PK, 자동 생성)                                                     |
| `name`          | String           | 상품명 (최대 100자, 필수)                                                        |
| `description`   | String           | 상품 설명 (TEXT 타입, 상세 정보 저장)                                                |
| `price`         | BigDecimal       | 가격 (최대 10자리, 소수점 2자리, 필수)                                                |
| `stockQuantity` | Integer          | 재고 수량 (기본값 0, 필수)                                                        |
| `category`      | String           | 상품 카테고리 (최대 50자, 필수)                                                     |
| `imageUrl`      | String           | 상품 이미지 URL (최대 200자, 선택 입력)                                              |
| `createdAt`     | LocalDateTime    | 등록일시 (자동 생성, 수정 불가)                                                      |
| `updatedAt`     | LocalDateTime    | 수정일시 (자동 갱신)                                                             |
| `status`        | String           | 상품 상태 (AVAILABLE: 판매 가능, SOLD_OUT: 품절, DISCONTINUED: 단종, 기본값: AVAILABLE) |
| `carts`         | List<CartEntity> | 장바구니 목록 (1:N 관계, Cascade.ALL, orphanRemoval 적용)                          |

### 📘 장바구니 테이블 (CartEntity)
| 필드명         | 타입            | 설명                                        |
| ----------- | ------------- | ----------------------------------------- |
| `id`        | Long          | 장바구니 항목 고유 ID (PK, 자동 생성)                 |
| `member`    | MemberEntity  | 장바구니 소유 회원 (N:1 관계, 필수, `member_id` FK)   |
| `product`   | ProductEntity | 장바구니에 담긴 상품 (N:1 관계, 필수, `product_id` FK) |
| `quantity`  | Integer       | 상품 수량 (기본값 1, 필수)                         |
| `createdAt` | LocalDateTime | 장바구니에 담은 일시 (자동 생성, 수정 불가)                |


### 📘 시작 API 정의서 (IndexController)
| 기능         | Method | 경로  | 설명         | 요청 파라미터 / 요청 바디 | 반환 / 처리                          |
| ---------- | ------ | --- | ---------- | --------------- | -------------------------------- |
| **시작 페이지** | GET    | `/` | 메인 페이지로 이동 | 없음              | `index.html` 뷰 렌더링 / 세션 정보 전달 가능 |

### 📘 회원 API 정의서 (MemberController)
| 기능                 | Method | 경로                         | 설명                               | 요청 파라미터 / 요청 바디                            | 반환 / 처리                                                                 |
| ------------------ | ------ | -------------------------- | -------------------------------- | ------------------------------------------ | ----------------------------------------------------------------------- |
| **회원 목록 조회**       | GET    | `/members`                 | 모든 회원 조회                         | 없음                                         | `member/list.html` 뷰 렌더링, `members` 모델 전달                               |
| **회원 가입 폼**        | GET    | `/members/register`        | 회원 가입 입력 폼 페이지                   | 없음                                         | `member/register.html` 뷰 렌더링, 빈 `MemberDTO` 모델 전달                       |
| **회원 가입 처리**       | POST   | `/members/register`        | 회원 가입 처리 후 로그인 페이지로 이동           | Form Data: `MemberDTO`                     | 성공 시 `/members/login`로 리다이렉트, 실패 시 `/members/register?error=메시지`로 리다이렉트 |
| **로그인 폼**          | GET    | `/members/login`           | 로그인 입력 폼 페이지                     | 없음                                         | `member/login.html` 뷰 렌더링                                               |
| **로그인 처리**         | POST   | `/members/login`           | 로그인 처리 후 메인 페이지로 이동              | RequestParam: `username`, `password`       | 성공 시 `/`로 리다이렉트, 실패 시 `member/login.html` 재표시, `error` 모델 전달            |
| **회원 정보 조회**       | GET    | `/members/{id}`            | 특정 회원 상세 정보 조회                   | PathVariable: `id`                         | `member/view.html` 뷰 렌더링, `member` 모델 전달                                |
| **회원 정보 수정 폼**     | GET    | `/members/edit/{id}`       | 회원 정보 수정 입력 폼 페이지                | PathVariable: `id`                         | `member/edit.html` 뷰 렌더링, `member` 모델 전달                                |
| **회원 정보 수정 처리**    | POST   | `/members/edit/{id}`       | 회원 정보 수정 처리 후 상세 페이지로 이동         | PathVariable: `id`, Form Data: `MemberDTO` | `/members/{id}`로 리다이렉트                                                  |
| **회원 탈퇴 (소프트 삭제)** | GET    | `/members/delete/{id}`     | 회원 탈퇴 처리 후 회원 목록 페이지로 이동         | PathVariable: `id`                         | `/members`로 리다이렉트                                                       |
| **마이페이지 조회**       | GET    | `/members/mypage/{id}`     | 로그인 회원 마이페이지 조회                  | PathVariable: `id`                         | `member/mypage.html` 뷰 렌더링, `member` 모델 전달                              |
| **상태별 회원 조회**      | GET    | `/members/status/{status}` | 특정 상태 회원 조회 (ACTIVE, INACTIVE 등) | PathVariable: `status`                     | `member/list.html` 뷰 렌더링, `members` 및 `status` 모델 전달                    |

### 📘 상품 API 정의서 (ProductController)
| 기능               | Method | 경로                              | 설명                       | 요청 파라미터 / 요청 바디                              | 반환 / 처리                                                             |
| ---------------- | ------ | ------------------------------- | ------------------------ | -------------------------------------------- | ------------------------------------------------------------------- |
| **상품 목록 조회**     | GET    | `/products`                     | 모든 상품 조회                 | 없음                                           | `product/list.html` 뷰 렌더링, `products` 모델 전달                         |
| **상품 등록 폼**      | GET    | `/products/new`                 | 상품 등록 입력 폼 페이지           | 없음                                           | `product/create.html` 뷰 렌더링, 빈 `ProductDTO` 모델 전달                   |
| **상품 등록 처리**     | POST   | `/products`                     | 상품 등록 처리 후 상품 목록 페이지로 이동 | Form Data: `ProductDTO`                      | 성공 시 `/products`로 리다이렉트, 실패 시 `/products/new?error=메시지`로 리다이렉트      |
| **상품 상세 조회**     | GET    | `/products/{id}`                | 특정 상품 상세 페이지 조회          | PathVariable: `id`                           | `product/view.html` 뷰 렌더링, `product` 모델 전달                          |
| **상품 수정 폼**      | GET    | `/products/edit/{id}`           | 상품 수정 입력 폼 페이지           | PathVariable: `id`                           | `product/edit.html` 뷰 렌더링, `product` 모델 전달                          |
| **상품 수정 처리**     | POST   | `/products/edit/{id}`           | 상품 수정 처리 후 상세 페이지로 이동    | PathVariable: `id`, Form Data: `ProductDTO`  | 성공 시 `/products/{id}`로 리다이렉트, 실패 시 `/products/edit/{id}?error=메시지`  |
| **상품 삭제**        | GET    | `/products/delete/{id}`         | 상품 삭제 처리 후 상품 목록 페이지로 이동 | PathVariable: `id`                           | `/products`로 리다이렉트                                                  |
| **카테고리별 상품 조회**  | GET    | `/products/category/{category}` | 특정 카테고리 상품 조회            | PathVariable: `category`                     | `product/list.html` 뷰 렌더링, `products` 및 `category` 모델 전달            |
| **상품 검색**        | GET    | `/products/search`              | 키워드로 상품 검색               | RequestParam: `keyword`                      | `product/list.html` 뷰 렌더링, `products` 및 `keyword` 모델 전달             |
| **가격 범위 상품 조회**  | GET    | `/products/price-range`         | 최소~최대 가격 범위로 상품 조회       | RequestParam: `minPrice`, `maxPrice`         | `product/list.html` 뷰 렌더링, `products`, `minPrice`, `maxPrice` 모델 전달 |
| **재고 있는 상품만 조회** | GET    | `/products/available`           | 재고가 있는 상품만 조회            | 없음                                           | `product/list.html` 뷰 렌더링, `products` 모델 전달                         |
| **재고 관리 폼**      | GET    | `/products/stock/{id}`          | 특정 상품 재고 관리 폼 조회         | PathVariable: `id`                           | `product/stock.html` 뷰 렌더링, `product` 모델 전달                         |
| **재고 증가**        | POST   | `/products/stock/increase/{id}` | 상품 재고 증가 처리              | PathVariable: `id`, RequestParam: `quantity` | `/products/{id}`로 리다이렉트                                             |
| **재고 감소**        | POST   | `/products/stock/decrease/{id}` | 상품 재고 감소 처리              | PathVariable: `id`, RequestParam: `quantity` | 성공 시 `/products/{id}`로 리다이렉트, 실패 시 `/products/stock/{id}?error=메시지` |

### 📘 장바구니 API 정의서 (CartController)
| 기능                       | Method | 경로                       | 설명                          | 요청 파라미터 / 요청 바디                                           | 반환 / 처리                                                                |
| ------------------------ | ------ | ------------------------ | --------------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------- |
| **장바구니 조회**              | GET    | `/cart/{memberId}`       | 특정 회원 장바구니 목록 조회 및 총 금액 확인  | PathVariable: `memberId`                                  | `cart/view.html` 뷰 렌더링, `cartItems`, `totalPrice`, `memberId` 모델 전달    |
| **장바구니에 상품 추가**          | POST   | `/cart/add`              | 특정 회원 장바구니에 상품 추가           | RequestParam: `memberId`, `productId`, `quantity` (기본값 1) | 성공 시 `/cart/{memberId}`로 리다이렉트, 실패 시 `/products/{productId}?error=메시지` |
| **장바구니 수량 변경**           | POST   | `/cart/update/{id}`      | 장바구니 항목 수량 변경               | PathVariable: `id`, RequestParam: `quantity`, `memberId`  | 성공 시 `/cart/{memberId}`로 리다이렉트, 실패 시 오류 메시지 포함 리다이렉트                   |
| **장바구니 항목 삭제**           | GET    | `/cart/delete/{id}`      | 특정 장바구니 항목 삭제               | PathVariable: `id`, RequestParam: `memberId`              | `/cart/{memberId}`로 리다이렉트                                              |
| **장바구니 전체 비우기**          | GET    | `/cart/clear/{memberId}` | 특정 회원 장바구니 전체 삭제            | PathVariable: `memberId`                                  | `/cart/{memberId}`로 리다이렉트                                              |
| **장바구니 수량 증가**           | POST   | `/cart/increase/{id}`    | 장바구니 항목 수량 1 증가             | PathVariable: `id`, RequestParam: `memberId`              | 성공 시 `/cart/{memberId}`로 리다이렉트, 실패 시 오류 메시지 포함 리다이렉트                   |
| **장바구니 수량 감소**           | POST   | `/cart/decrease/{id}`    | 장바구니 항목 수량 1 감소 (1이면 삭제 처리) | PathVariable: `id`, RequestParam: `memberId`              | 성공 시 `/cart/{memberId}`로 리다이렉트, 실패 시 오류 메시지 포함 리다이렉트                   |
| **장바구니 항목 개수 조회 (Ajax)** | GET    | `/cart/count/{memberId}` | 특정 회원 장바구니 항목 개수 조회 (Ajax)  | PathVariable: `memberId`                                  | 정수값 반환 (`int`)                                                         |
| **장바구니 총 금액 조회 (Ajax)**  | GET    | `/cart/total/{memberId}` | 특정 회원 장바구니 총 금액 조회 (Ajax)   | PathVariable: `memberId`                                  | 총 금액 반환 (`BigDecimal`)                                                 |


### 📘 회원 로직 정의서 (MemberService)
| 함수 이름                 | 매개변수                               | 반환형               | 설명                                  | 특징 / 비고                                                                |
| --------------------- | ---------------------------------- | ----------------- | ----------------------------------- | ---------------------------------------------------------------------- |
| `createMember`        | `MemberDTO dto`                    | `MemberDTO`       | 신규 회원 가입 처리 후 DTO 반환                | 사용자명/이메일 중복 체크, 중복 시 `IllegalStateException` 발생; 비밀번호 암호화, 상태 `ACTIVE` |
| `getMember`           | `Long id`                          | `MemberDTO`       | 회원 단건 조회 후 DTO 반환                   | 존재하지 않으면 `IllegalStateException` 발생                                    |
| `getAllMembers`       | 없음                                 | `List<MemberDTO>` | 모든 회원 조회 후 DTO 리스트 반환               | Entity → DTO 변환 수행                                                     |
| `getMemberByUsername` | `String username`                  | `MemberDTO`       | 사용자명 기준 회원 조회 후 DTO 반환              | 존재하지 않으면 `IllegalStateException` 발생                                    |
| `getMembersByStatus`  | `String status`                    | `List<MemberDTO>` | 상태별 회원 조회 후 DTO 리스트 반환              | 상태(`ACTIVE`, `INACTIVE`, `DELETED`) 기준 조회                              |
| `updateMember`        | `Long id, MemberDTO dto`           | `MemberDTO`       | 회원 정보 수정 후 DTO 반환                   | 비밀번호 변경 시만 업데이트, 존재 여부 확인, 상태 변경 가능                                    |
| `deleteMember`        | `Long id`                          | `boolean`         | 회원 소프트 삭제 (상태 `DELETED`) 후 성공 여부 반환 | 실제 데이터 삭제 없이 상태만 변경                                                    |
| `hardDeleteMember`    | `Long id`                          | `boolean`         | 회원 실제 삭제 처리 후 성공 여부 반환              | 존재하지 않으면 `false` 반환                                                    |
| `login`               | `String username, String password` | `MemberDTO`       | 로그인 검증 후 DTO 반환                     | 사용자명/비밀번호 불일치 시 `IllegalStateException` 발생, 상태가 `ACTIVE`여야 로그인 가능      |
| `mapToDTO` (private)  | `MemberEntity member`              | `MemberDTO`       | Entity → DTO 변환                     | 비밀번호 제외, 장바구니 항목 수 포함                                                  |

### 📘 상품 로직 정의서 (ProductService)
| 함수 이름                     | 매개변수                                       | 반환형                | 설명                         | 특징 / 비고                                                                |
| ------------------------- | ------------------------------------------ | ------------------ | -------------------------- | ---------------------------------------------------------------------- |
| `createProduct`           | `ProductDTO dto`                           | `ProductDTO`       | 신규 상품 등록 후 DTO 반환          | 가격 ≥ 0, 재고 ≥ 0 검증; Entity → DTO 변환; 예외 발생 시 `IllegalArgumentException` |
| `getProduct`              | `Long id`                                  | `ProductDTO`       | 단건 상품 조회 후 DTO 반환          | 존재하지 않으면 `IllegalStateException` 발생                                    |
| `getAllProducts`          | 없음                                         | `List<ProductDTO>` | 전체 상품 조회 후 DTO 리스트 반환      | Entity → DTO 변환 수행                                                     |
| `getProductsByCategory`   | `String category`                          | `List<ProductDTO>` | 카테고리 기준 상품 조회 후 DTO 리스트 반환 | Entity → DTO 변환                                                        |
| `getProductsByStatus`     | `String status`                            | `List<ProductDTO>` | 상태 기준 상품 조회 후 DTO 리스트 반환   | `AVAILABLE`, `SOLD_OUT`, `DISCONTINUED` 등                              |
| `getProductsByPriceRange` | `BigDecimal minPrice, BigDecimal maxPrice` | `List<ProductDTO>` | 가격 범위 상품 조회 후 DTO 리스트 반환   | Entity → DTO 변환                                                        |
| `searchProducts`          | `String name`                              | `List<ProductDTO>` | 이름 포함 검색 후 DTO 리스트 반환      | Entity → DTO 변환                                                        |
| `getAvailableProducts`    | 없음                                         | `List<ProductDTO>` | 재고 있는 상품 조회 후 DTO 리스트 반환   | `stockQuantity > 0` 기준 조회                                              |
| `updateProduct`           | `Long id, ProductDTO dto`                  | `ProductDTO`       | 상품 정보 수정 후 DTO 반환          | 가격/재고 유효성 검증; 존재 여부 확인; 상태 변경 가능                                       |
| `increaseStock`           | `Long id, Integer quantity`                | `ProductDTO`       | 재고 수량 증가 후 DTO 반환          | 재고 양 계산 후 저장                                                           |
| `decreaseStock`           | `Long id, Integer quantity`                | `ProductDTO`       | 재고 수량 감소 후 DTO 반환          | 재고 부족 시 `IllegalStateException` 발생                                     |
| `deleteProduct`           | `Long id`                                  | `boolean`          | 상품 소프트 삭제 후 성공 여부 반환       | 상태를 `DISCONTINUED`로 변경                                                 |
| `hardDeleteProduct`       | `Long id`                                  | `boolean`          | 상품 실제 삭제 처리 후 성공 여부 반환     | 존재하지 않으면 `false` 반환                                                    |
| `mapToDTO` (private)      | `ProductEntity product`                    | `ProductDTO`       | Entity → DTO 변환            | 장바구니 담긴 횟수(`cartCount`) 포함                                             |

### 📘 장바구니 로직 정의서 (CartService)
| 함수 이름                | 매개변수                        | 반환형             | 설명                     | 특징 / 비고                                                                                |
| -------------------- | --------------------------- | --------------- | ---------------------- | -------------------------------------------------------------------------------------- |
| `addToCart`          | `CartDTO dto`               | `CartDTO`       | 장바구니에 상품 추가 후 DTO 반환   | - 회원/상품 존재 여부 확인<br>- 재고 확인<br>- 이미 존재하면 수량 증가<br>- 재고 부족 시 `IllegalStateException` 발생 |
| `getCartByMember`    | `Long memberId`             | `List<CartDTO>` | 특정 회원의 장바구니 목록 조회      | 회원 존재 여부 확인<br>Entity → DTO 변환                                                         |
| `getCart`            | `Long id`                   | `CartDTO`       | 단일 장바구니 항목 조회          | 존재하지 않으면 `IllegalStateException` 발생                                                    |
| `updateQuantity`     | `Long id, Integer quantity` | `CartDTO`       | 장바구니 항목 수량 변경 후 DTO 반환 | 수량 ≥ 1 검증<br>재고 부족 시 `IllegalStateException` 발생                                        |
| `deleteCart`         | `Long id`                   | `boolean`       | 단일 장바구니 항목 삭제          | 존재 여부 확인, 삭제 성공 시 `true`, 없으면 `false`                                                  |
| `clearCart`          | `Long memberId`             | `boolean`       | 특정 회원의 장바구니 전체 삭제      | 회원 존재 여부 확인<br>삭제 성공 시 `true`                                                          |
| `getTotalPrice`      | `Long memberId`             | `BigDecimal`    | 특정 회원 장바구니 총 금액 계산     | `product.price × quantity` 합계                                                          |
| `getCartCount`       | `Long memberId`             | `int`           | 특정 회원 장바구니 항목 개수 조회    | DB에서 `count` 조회                                                                        |
| `mapToDTO` (private) | `CartEntity cart`           | `CartDTO`       | Entity → DTO 변환        | - 회원/상품 정보 포함<br>- 총 가격 계산 포함<br>- 장바구니 항목 단건 변환 전용                                    |

### 📘 회원 쿼리 정의서 (MemberRepository)
| 메서드 이름                 | JPQL / SQL 동작 설명                 | 입력 파라미터           | 반환 타입                    | 특징 / 비고                                 |
| ---------------------- | -------------------------------- | ----------------- | ------------------------ | --------------------------------------- |
| `findByUsername`       | 특정 사용자명(`username`)에 해당하는 회원 조회  | `String username` | `Optional<MemberEntity>` | 단건 조회, 로그인 검증 등에 사용                     |
| `findByEmail`          | 특정 이메일(`email`)에 해당하는 회원 조회      | `String email`    | `Optional<MemberEntity>` | 단건 조회, 이메일 중복 체크 등에 사용                  |
| `findByStatus`         | 특정 상태(`status`)를 가진 회원 리스트 조회    | `String status`   | `List<MemberEntity>`     | 상태 값 예: `ACTIVE`, `INACTIVE`, `DELETED` |
| `findByNameContaining` | 회원 이름(`name`)에 특정 문자열이 포함된 회원 조회 | `String name`     | `List<MemberEntity>`     | 부분 검색, 대소문자 민감도는 DB 설정에 따라 다름           |
| `existsByUsername`     | 특정 사용자명(`username`) 존재 여부 확인     | `String username` | `boolean`                | 회원 가입 시 사용자명 중복 체크                      |
| `existsByEmail`        | 특정 이메일(`email`) 존재 여부 확인         | `String email`    | `boolean`                | 회원 가입 시 이메일 중복 체크                       |

### 📘 상품 쿼리 정의서 (ProductRepository)
| 메서드 이름                           | JPQL / SQL 동작 설명                     | 입력 파라미터                                      | 반환 타입                 | 특징 / 비고                                         |
| -------------------------------- | ------------------------------------ | -------------------------------------------- | --------------------- | ----------------------------------------------- |
| `findByNameContaining`           | 상품명(`name`)에 특정 문자열이 포함된 상품 조회       | `String name`                                | `List<ProductEntity>` | 부분 검색, 대소문자 민감도는 DB 설정에 따라 다름                   |
| `findByCategory`                 | 특정 카테고리(`category`)의 상품 조회           | `String category`                            | `List<ProductEntity>` | 카테고리 정확히 일치                                     |
| `findByStatus`                   | 상품 상태(`status`)로 상품 조회               | `String status`                              | `List<ProductEntity>` | 상태 값 예: `AVAILABLE`, `SOLD_OUT`, `DISCONTINUED` |
| `findByPriceBetween`             | 가격(`price`)이 최소~최대 범위 사이인 상품 조회      | `BigDecimal minPrice`, `BigDecimal maxPrice` | `List<ProductEntity>` | 범위 포함 (`>= minPrice` && `<= maxPrice`)          |
| `findByStockQuantityGreaterThan` | 재고(`stockQuantity`)가 특정 수량보다 큰 상품 조회 | `Integer quantity`                           | `List<ProductEntity>` | quantity 초과 상품만 조회                              |
| `findByCategoryAndStatus`        | 특정 카테고리와 상태를 동시에 만족하는 상품 조회          | `String category`, `String status`           | `List<ProductEntity>` | AND 조건 결합                                       |

### 📘 장바구니 쿼리 정의서 (CartRepository)
| 메서드 이름                         | JPQL / SQL 동작 설명                   | 입력 파라미터                           | 반환 타입                  | 특징 / 비고            |
| ------------------------------ | ---------------------------------- | --------------------------------- | ---------------------- | ------------------ |
| `findByMemberId`               | 특정 회원(`memberId`)의 장바구니 항목 전체 조회   | `Long memberId`                   | `List<CartEntity>`     | 회원별 장바구니 조회용       |
| `findByProductId`              | 특정 상품(`productId`)이 포함된 장바구니 항목 조회 | `Long productId`                  | `List<CartEntity>`     | 상품별 장바구니 현황 확인용    |
| `findByMemberIdAndProductId`   | 특정 회원의 특정 상품 장바구니 항목 조회            | `Long memberId`, `Long productId` | `Optional<CartEntity>` | 장바구니 중복 항목 확인용     |
| `deleteByMemberId`             | 특정 회원(`memberId`)의 장바구니 전체 삭제      | `Long memberId`                   | `void`                 | 회원 탈퇴 또는 장바구니 초기화용 |
| `countByMemberId`              | 특정 회원(`memberId`) 장바구니 항목 개수 조회    | `Long memberId`                   | `int`                  | 장바구니 아이템 수 표시용     |
| `existsByMemberIdAndProductId` | 특정 회원이 특정 상품을 이미 장바구니에 담았는지 확인     | `Long memberId`, `Long productId` | `boolean`              | 장바구니 중복 체크용        |
